Hello world or something like that
Commonly used git commands

---------------------------
I'll keep below for now (TODO:) - but note a couple things:
Default for repos changed from "master" to "main"
I should change my conventions to not use "main" as default for upstream since that's confusing
Use upstream instead from now on
And note you'll have to always check if remotes are main or master
---------------------------

git branch -av
git remote -v
git remote update -p
git push origin main/master:master; git pull

use checkout to initialize branches

New repo needs an initial commit after using git init

After cloning repo, to set up upstream with different name than local
git push -u origin master:main

When it already exists:
git branch --set-upstream-to=origin/main master

To set default to pushing to upstream for above:
git config --global push.default upstream

TODO: Add notes for log, rebase, etc.
