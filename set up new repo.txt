to do from existing project:

create repo on website, then locally:

git init
git remote add origin __________.git

if you started with a readme on website on an empty repo, 
you have to pull first. otherwise you get a merge issue
i.e. git pull origin master

add any file and make a commit
git push --set-upstream origin master
